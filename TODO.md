- [ ] Atualizar `services/aluno-service/src/test/resources/application-test.properties` para apontar corretamente (sem metadados/cabeçalhos) e garantir configuração de Redis host/port quando necessário.
- [ ] Atualizar `.github/workflows/ci.yml` para: 
  - [ ] aguardar a porta 6379 antes dos testes do aluno-service
  - [ ] forçar `spring.redis.host=127.0.0.1` (sem localhost/IPv6)
- [ ] Verificar `git status` e `git diff` dos arquivos alterados
- [ ] Rodar testes localmente (opcional)
